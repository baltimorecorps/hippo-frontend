import {createMuiTheme} from '@material-ui/core/styles';

let mockTheme = createMuiTheme({
  palette: {
    resume: {
      yellow: '#f3ba4d',
      blue: '#1c4587',
      gray: '#e7e6e6',
    },
  },
});

export {mockTheme};
